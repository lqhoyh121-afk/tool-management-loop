# T01 管理人实际完成取证与反向权限待测

> 历史阶段记录：当时状态已由 [最终交接报告](T01-HANDOFF.md) 更新；不作为当前验收或资源状态。

## 独立办理确认已取证

真人完成独立发放确认待办后，按清单中精确 taskId 回读，与办理前回执比较：isDone 由 false 变 true，finishTime 有值；新增 task.self.done 与 task.done 活动，各有活动ID，两项实际活动 creatorId 均与指定管理人内部执行者ID精确一致。不能只以 modifierId 或布尔值判定，本次采用前后活动差异。

再次独立查询原申请与单笔审批表，各一条记录，精确记录ID与清单匹配；审批单选仍为同意，办理标题原申请引用一致。申请、决定和独立完成三个环节已分别取得真实证据。已保存原始事件和时间于本机清单，公开文档不含业务标识。

上述为单笔串行平台能力实测，不证明通用外键、不可篡改关联、并发或业务自动状态机。隔离预留仍保留待清理，没有改正式借出台账，没有宣称实物交付。

## 权限反向准备

另建一条专用于非指定人尝试的隔离待办，仅指定管理人为执行者，与真实办理测试待办分离；创建前防重，创建后精确回读 taskId、标题、执行者内部ID及未完成状态。它不驱动库存或批准。

拟由原申请人本人在其钉钉会话查看单笔审批页、主 Base、专用权限待办。审批/主账如出现可操作入口，先反馈不代审批、不改库存；仅允许尝试完成专用权限待办。须结合本人错误提示/截图和后台记录变化取证，不以管理员视角代替普通账号测试。

此刻反向结果及测试资源清理未完成，L4 不可验收。后续须回读上述三个精确目标，再决定继续测试或清理；全部资源复用私有清单，禁止重复新建。未提交、推送、合并或关闭 Issue。
