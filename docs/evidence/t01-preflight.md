# T01 钉钉真实链路：开工核验与待输入

> 历史阶段记录：当时状态已由 [最终交接报告](T01-HANDOFF.md) 更新；不作为当前验收或资源状态。

## 范围与当前结论

仅处理 GitHub Issue #1，依据该 Issue 最新派工评论。当前停在测试目标及真人角色确认之前，未完成 L4 验收，不释放后续公共约定冻结门槛。

- 已核验指定工作目录与仓库根目录一致。
- 分支：`task/t01-dingtalk-validation`。
- HEAD：`5ac1266b786eddded2334d5076de401ec46238f1`，与固定基线一致。
- 开工时工作区干净。
- 已读 AGENTS.md、SPEC.md、design.md、tickets.md。
- 已通过 Issue GET 的评论总数与分页评论枚举交叉核对，开工时共两条评论，以最后一条派工评论为准。

## 本次真实只读核验

| 检查 | 结果 | 证据来源与边界 |
|---|---|---|
| 当前授权状态 | 已验证已认证、令牌有效 | 本机 `dws auth status --format json`，仅消费状态，不公开凭据或标识 |
| 当前接口登录身份 | 已验证两个入口身份一致 | `dws contact user me -y` 返回一条当前人员记录；`result[].orgEmployeeModel.userId` 与认证状态 `user_id` 精确一致；姓名与组织仅在本机会话核对 |
| 使用者确认本轮身份绑定 | 待确认 | 实际身份已展示给使用者；不从历史缓存绑定参与角色 |

只读身份响应在内存中解析，未将原始回执、个人标识或凭据写入仓库文件。已认证不证明收集表提交人、审批人或待办真实完成者可信。

## L4 验收状态

| 验收项 | 状态 | 缺少条件或证据 |
|---|---|---|
| 收集表提交及可信申请身份 | 未验证／待输入 | 授权隔离收集表与结果表、真人申请人、测试数据；需真实提交回读 |
| 明确同意与拒绝结果及原单关联 | 未验证／待输入 | 审批入口、授权审批人、真实人工处理及可信结果来源 |
| 仅勾审批待办不能视为同意 | 未验证 | 需真人完成该反向动作并回读审批结果，不由 Agent 代勾 |
| 指定管理人确认及真实完成者 | 未验证／待输入 | 管理人和关联待办、真实人工确认、实际操作者证据；指定执行人不等同完成者 |
| 普通使用者字段权限及主账隔离 | 未验证／待输入 | 非管理权限真人账号与隔离目标；当前账号具有管理标签，不能代表普通使用者权限 |
| 错人不放行与错单关联 | 未验证／待输入 | 经授权的非指定参与者、隔离反向用例及精确回读 |
| 外部写入结果不明恢复与重复运行 | 未验证 | 当前未写入；后续先回查原目标，不盲目重发 |
| 测试资源清理并回读 | 未验证／未发生 | 当前未创建或更改钉钉测试资源，无本轮新增资源需要清理；不等同清理能力通过 |

## 集中待确认输入

1. 确认实时读出的登录身份为本轮授权账号。
2. 指定隔离收集表、结果表／主账及审批处理入口；若没有现成目标，明确是否授权建立一次性沙箱及测试后清理。
3. 指定真实申请人、审批人、管理人及用于错人／普通权限验证的参与者；由真人操作自己的账号，不共享凭据。
4. 提供测试工具引用、数量、预计归还时间；如按单件追踪，还需实物编号。缺少真实输入不生成替代数据冒充真实验收。
5. 确认原始证据本地保管位置与访问保护。当前仅允许写脱敏结论，未建立额外原始证据目录；如需保存原始回执，先明确授权并核验 Git 忽略与本地保护。

## 边界与交回

- 未开发探针或业务源码，未开展埋点及其他任务。
- 未代为审批、勾选完成或修改生产记录。
- 未提交、推送、合并或关闭 Issue。
- 后续接口字段只记录真实观测形态；缺真实操作者证据则继续阻塞，必要时向主控申请独立探针范围或提交替代设计，不降低门槛。
- 本文件是阶段性待输入记录，不是最终验收报告；最终交回主控验收。
