# T01 台账只读核验与数据阻塞

> 历史阶段记录：当时状态已由 [最终交接报告](T01-HANDOFF.md) 更新；不作为当前验收或资源状态。

## 已确认输入

- 使用者确认原始台账及回执仅在本机保管，公开仓库仅保存脱敏结论。
- 申请工具为螺丝刀，数量 1 件；预计归还时间已按使用者当次“今天18点”解析为北京时间，并在私有会话确认。公开报告不保存人员及业务标识。
- 本轮申请人与审批／管理人的角色已经确认，审批人和管理人为同一真人；两个动作仍需分别取证。

## 原文件核验

附件扩展名为 `.xls`，文件内容实际为 HTML 表格。通用文档读取失败后，使用本机现有 HTML 解析库只读解析，未改原文件或导入全量台账。

按“螺丝刀”检索物资行，只有一条匹配：一字螺丝刀，规格 2.4mm，发放单位为把，当前余量 0.0，当前预留数量 0.0。该值是所提供导出台账的值，不证明实时实物库存。

## 阻塞与边界

- 使用者请求借 1 件，但所提供匹配物资余量为零，不能把零改成一或宣称可以正常借出。
- 待使用者决定：保留零库存测试拒绝路径，或明确授权在隔离测试表设置专用测试库存以跑正常链。专用测试库存必须标明为人工授权的测试夹具，不冒充真实库存或生产业务验收。
- 当前未创建钉钉测试资源、发送待办或更改正式库存。
- 真实收集表提交、审批结果、管理人完成者身份、权限隔离、关联回读及清理仍未验证。
