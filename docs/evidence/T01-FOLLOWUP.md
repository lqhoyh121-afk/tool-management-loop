# T01 定向补测交接（待主控验收）

Refs #1

## 基线、授权与边界

以 `7162868b8f84d75c37637dc160672d23fcf9d241` 建独立分支 `task/t01-permission-followup`，依据 Issue #1 主控定向补测评论。保留之前最终交接，不重跑已验正常链，不修改正式台账，不代做人工作业。

使用者当次明确授权本轮新沙箱创建与清理，指定另一名真人为跨创建者执行人及不同专用目标的非指定普通测试者；拒绝审批仍由本人处理，通知仅本人及指定参与者单聊。真实姓名、标识及业务链接不公开。

本轮原始回执独立存于本机私有目录，已移除目录继承并回读 ACL，仅当前用户和 SYSTEM 具有完全权限，Git 忽略已核验。原始证据和备份不进入公开提交。

## 当前事实

- 实时核对本人认证有效；按本轮指定姓名查得唯一联系人，不复用上一轮角色。
- 已创建一条仅指定另一名真人执行的专用待办，保存创建参数、当次联系人信息及初始精确回读。
- 真人完成后对同一 taskId 精确回读：isDone 从 false 变 true，新增 task.self.done 与 task.done 事件。两事件活动 creatorId 均等于唯一执行者内部ID，且不同于创建人内部ID；初始及最终 executorIds 一致。
- 身份绑定依据为当次唯一通讯录人员 userId、保存的 create --executors 精确参数、该创建响应对应的 taskId、初始唯一执行者内部ID、最终实际事件操作者ID。再次实时查通讯录并核对同一 userId，不以姓名展示、modifierId 或创建人推断完成者。这是本轮参数到唯一执行者的实际关联证据，不外推为所有平台事件或通用身份转换接口。
- 另建只分配给管理人的独立未完成待办，已精确回读初始状态；非指定测试者使用的是这条新目标，不是已完成任务。已收到该项原始权限截图；不把其他两项转述提升为直接观察。
- 未创建借用正常链、预留或发放记录，未代勾。

## 定向验收状态

已接收原始权限截图，直接可见“抱歉，你无权限操作”，并保存在本机保护目录，复制前后摘要一致。图片不显示账号或任务标识，人员和目标绑定来自此前专用链接单聊及使用者明确确认，不夸大成图片自带完整身份链。对应专用待办精确回读仍未完成、活动未变。

本轮新建独立隔离 Base，主账仅使用明确标注的合成非业务记录。分享面板为仅文档协作者可访问，未给普通测试者扩权；管理账号对合成受保护字段完成读、改、回读及恢复核验。该结果不等于字段级角色权限配置通过，主账整体拒绝方案仍待主控是否接受为首版权限替代设计。

另建合成独立拒绝原单及受限审批入口，原单引用配置已回读，只有指定审批人可填写，匿名与提交后修改关闭；必填单选含同意和拒绝、系统 creator/createdTime，真人操作前结果表为空。随后精确查询到一条明确拒绝记录，系统 creator 的人员及组织与当次本人认证一致，createdTime 可解析，表单配置中的原单引用与独立原单记录一致。全程未执行预留或发放；这不是尚未开发的自动放行闸门测试。

使用者明确确认普通测试者审批页显示无需填写、主账显示无权限，并要求继续收尾。这两项未收到原始截图，故仍只记真人反馈；后台主账合成记录与初始值一致，审批仅有指定人拒绝记录，专用权限待办仍未完成且活动未变。继续收尾不等于使用者或主控豁免直接证据门槛。

| 项目 | 当前状态 |
|---|---|
| 跨创建者完成身份 | 本轮实际完成事件及创建参数绑定已核验，待主控审查证据；不外推通用映射 |
| 非指定人直接拒绝证据 | 待办原始错误截图+用户身份目标确认+后台未变；审批及主账缺直接截图，未完整满足直接证据要求 |
| 权限边界 | 管理账号合成字段读改恢复已验；普通人全拒绝为反馈加后台未变。字段级角色权限未验；替代设计待主控确认 |
| 明确拒绝分支 | 真人拒绝结果、系统身份、时间及独立原单配置已回读；不驱动预留或发放 |
| 清理回读 | 两条专用待办及一个隔离 Base 已删除并精确回查；分享表单实际显示已删除；非永久物理清除 |

## 清理与证据保管

本轮独立清单包含两个专用待办及一个 Base，删除前精确读回核验归属。每条待办有成功删除回执，随后同一 ID 查询返回 `task not exist or operator not in task`。该错误不能单独证明删除；本次结合此前可读、明确成功删除响应和事后同目标错误保留证据。跨创建者待办不在创建人作为执行者的列表覆盖范围内，因此不虚构创建人列表排除证据；证据是否足够由主控判定。

Base 含五张表（包括平台默认表），按本轮精确 Base ID 整体删除。删除响应与独立 get 的 `BASE_NOT_FOUND` 一致，同名搜索耗尽无目标；合成主账、拒绝入口及独立原单查询均返回 `INVALID_BASE_ID`。原拒绝分享链接重新打开可见“表单已删除”。未对回收站永久清空，未撤回聊天审计，未动正式台账或其他资源。

非凭据回执、原始权限截图、摘要及精确资源清单仅保留本机受保护且 Git 忽略目录。公开报告不含姓名、业务标识、原始图片、表链接、业务数量或精确业务时间。补测结束后不因报告返工自动重建资源或重发业务消息。

## 主控决定项

1. 审查跨创建者的参数绑定到唯一内部执行者及完成事件的证据，不外推为通用身份查询接口。
2. 待办图片本身不含身份和目标；审批与主账还缺直接图片，不能勾选整个直接权限证据项完成。补齐需要独立授权的定向补测。
3. 本次普通人主账全拒绝、管理账号读改受保护字段的方案，仅作为首版权限替代建议；字段级角色细分未验证，须主控明确是否接受，不能自行降门槛。
4. 明确拒绝来源是受限原生表单，不是 OA；原单说明为受控配置关联，不是不可篡改外键。并发、断网、重启及业务程序闸门均未测，未扩展此次范围。

完整 L4 仍未通过。缺条件时记录，不扩展到业务源码、并发、断网或部署。最终仅交主控复审，不自行关闭 Issue 或合并。
