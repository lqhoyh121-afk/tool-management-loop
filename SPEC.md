# 需求与验收

## 1. 范围

总目标为个人领用、公用借还、以旧换新、报废的线上闭环。MVP只实现公用借还，其余留后续。钉钉收集表是申请入口，多维表是业务主账，待办承载人工确认。只收线上操作，不组织线下交接，不强制照片。没有真实观测来源时，不宣称数字孪生或实物实时状态。

首版固定为一台使用者电脑、一个运行账号、单实例、一套主账、一笔完整借还。普通人主账全拒绝，仅使用受限申请/审批入口；管理账号维护主账。不建设通用平台。范围依据 [Issue #2 使用者确认](https://github.com/lqhoyh121-afk/tool-management-loop/issues/2#issuecomment-5660798328)及[最新主控开工卡](https://github.com/lqhoyh121-afk/tool-management-loop/issues/2#issuecomment-5660952693)，优先于原宽范围草案。

## 2. 首版业务规则

1. 一单一种工具，可多件；普通工具按数量，需要追踪的工具按实物编号。缺编号标待补充，不将自动记录ID冒充实物编号。
2. 借用申请必须有可信提交身份、台账引用、数量及预计归还时间；字段来源来自真实输入，不能猜造。
3. 审批待办关联原单；通过处理链接获取明确同意或拒绝，不能把待办完成等同同意。
4. 同意后成功预留才能进入待借出确认；数量不可超额，单件不可重复预留。批准与预留冲突需显式挂起，不伪造成功。
5. 指定管理人完成关联的借出确认待办并核验实际操作身份后，才更新实际借出记录。仅尚未借出时，由创建配置中指定的管理人通过受限入口明确取消；已预留的必须释放并回读才能取消完成，不自动超时释放。未知写入未厘清前不接受取消旁路。
6. 提前1天提醒借用人，逾期每天一次；同一阶段防重。所有时间含时区，业务日期为Asia/Shanghai。发送时刻及跨日补发建议见contracts/README.md，主控确认前不启用提醒发送。
7. 归还申请必须关联原借用，整笔归还，数量及单件编号一致；不做分批归还。
8. 归还待确认暂停催借用人，不催管理员。管理人的必要确认待办仍需创建。
9. 核验指定管理人的完成证据后，更新台账、回读核验并关闭借用；确认前不恢复可借数量。
10. 同一个动作跨扫描及重启保留稳定身份，重复请求不重复办单。外部写入结果不明先回查，不盲目重建。
11. 在途单保留创建时配置版本及角色；新配置只用于新单。身份路由变化必须暂停并显式重新确认，不能静默改历史角色。具体重新绑定实现不在T02；未实现时保持停止。

## 3. 部署硬闸门

使用者双击BAT，程序显示终端面板，使用Windows文件选择框及官方授权页面。目标机自行准备环境，程序检测并列出缺项。环境、真实登录身份、台账、字段映射、审批及确认人员、读写权限分别核验；最终绑定由使用者明确确认。敏感信息不进日志或普通配置文件。

导入前展示字段及错误预览，用户确认后才创建/写入，导入后回读。支持真实Excel及HTML内容的xls导出文件。重复部署不得重复导入或覆盖已有业务。模板可自动生成，真实数量、人员、实物编号和资源归属不能由AI生成。

缺配置或权限时不能启动；所有业务入口均调用底层闸门，不依赖BAT、提示词或可编辑ready标记。配置失效重新核验，生产不设置跳过检查开关。系统管理员拥有源码或平台管理权时仍可能人为绕过，必须结合权限控制和审计，不承诺绝对防篡改。

## 4. 权责

- 使用者：授权、提供真实台账、确认部署绑定和人员。
- 申请人：借用及整笔归还申请。
- 审批人：明确同意/拒绝。
- 管理人：确认借出及归还；不得由脚本代勾。
- 主控：GitHub派工、接口冻结、本机测试、合并和发布决策。
- 协作者：隔离开发和测试，不操作主控机器、不索取主控凭据。

## 5. 先验证后依赖

T01已获主控接收为最小能力调查，足以启动T02隔离设计，不要求继续补齐完整字段级角色矩阵。历史未验证事实保留于docs/evidence/T01-HANDOFF.md和T01-FOLLOWUP.md：普通人审批/主账拒绝的部分证据为真人转述；通用审批关联、完整权限、故障恢复和完整L4未通过。

生产放行仍须在T07验证可信提交身份、明确审批结果、待办真实完成者、普通人主账全拒绝、读写回读及未知结果恢复。指定执行人不证明实际完成者；只有状态布尔值不足以放行。能力不成立则停止，不猜造外部能力。

可用明确标记的合成接口开发隔离模块，不将单测当真实接口通过。同一机器第二实例必须拦截，单写入者持排他权直到停止；运行时不允许管理人绕过流程并行直接改库存。不假设多维表提供数据库事务，预留冲突显式挂起；部分写入保持待回查。跨机器并发和迁移不在首版。

## 6. 验收合同

T02仅验L1纯契约及L2合成适配器/进程边界，不要求使用者参加真实测试。由主控审查并合并后才视为契约冻结，消费者不得把候选当已冻结依赖。之后T07在主控本机从最终仓库内容按首次部署路径进行真实整笔借还验收；通过前不生产放行。异机验收延期，不是首版闸门。

- 真实入口：BAT部署向导、钉钉借用与归还收集表、审批处理链接、管理人确认待办。
- 成功证据：真实记录ID、各阶段taskId、可信操作人、台账数量/编号/状态回读、提醒送达和不重复证据。
- 人工停止点：授权、台账及配置确认、审批、办理确认。缺输入不可代填。
- 反向验证：缺配置、错人、错单、拒绝不发放、数量不足、重复操作、预留冲突、第二实例、断网、结果不明、重启；归还未确认不恢复库存。
- 测试隔离：真实系统测试使用主控授权的隔离目标，测试记录可识别；清理后回读，不影响正式数据。
- 不完成：只有脚本退出码、模拟响应、文档检查或Agent自报成功。

## 7. 后续保留

个人领用、换新、报废、归还退回、延期、复杂异常以及网页维护、物资模块后置。当前异常不得因功能未实现而自动确认。规范采用版本化结构文件及条款来源，AI只提取草案，人确认、程序校验后发布；首版只保留读取和版本边界，不实现完整安全规则引擎，不显示未覆盖规则已合规。

细分字段权限、复杂角色矩阵、通用审批框架、跨机并发、异机迁移、网页平台、埋点均延期。T04不扩部署外壳，冻结后只补主链接入；T09只处理既有门禁问题。T02允许修改范围限contracts/、tests/contracts/及本文件、design.md、tickets.md，不改CI、检查脚本、依赖或他人模块。
