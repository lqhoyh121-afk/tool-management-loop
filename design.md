# 模块设计基线（实现前草案）

业务规则以SPEC.md为准。下列目录及接口是规划，不是已有业务程序。公共约定由主控冻结后分工，不建设跨平台协议或微服务集群。

## 模块所有权

| 模块 | 建议目录 | 职责 | 禁止越界 |
|---|---|---|---|
| 公共定义与校验 | contracts/ | 字段、状态、错误、操作标识、闸门契约 | 不藏平台调用或个人配置 |
| 钉钉读写 | integrations/dingtalk/ | 查询、写入、待办、结果回查、身份映射 | 不决定业务状态，不代审批 |
| 部署与导入 | bootstrap/ | BAT、可见检查、导入预览、绑定 | 不绕过共用读写，不内置真实数据 |
| 借还 | workflow/ | 预留、借出、归还、关联、幂等 | 不私建另一套调度或钉钉客户端 |
| 调度与提醒 | scheduler/ | 扫描、去重、恢复、提醒 | 不直接修改库存、不代人工确认 |
| 规则预留 | rules/ | 版本和来源、未覆盖的明确状态 | 不提前做完整制度引擎 |
| 验证及发布 | tests/、docs/ | 各层测试、证据、部署和迁移说明 | 不声称假接口证明外部成功 |

README、SPEC、design、依赖清单、CI和contracts定义由公共约定任务唯一维护。各协作者通过PR提出共用变更，不能各自扩私有字段。

## 状态与证据

候选主链：待审批→待预留核验→待借出确认→借用中→归还待确认→归还回写待核验→已关闭。拒绝、取消和技术待核验分支须在公共定义任务确定；没有证据时不能从中间状态跳到完成。

处理同一申请时校验原记录、阶段、配置版本、操作人；每个外部副作用绑定稳定operation_id。申请、归还记录、审批结果和阶段待办分别保存关联，不能只有一个通用完成标记。

## 写入和恢复

初版建议一个台账一个活动写入实例。一个进程内锁不能保护两个服务器，跨机器停旧启新及互斥策略属于必须验证项。在确定实现前，不宣称已解决分布式并发。

业务主账保持钉钉唯一权威；本地运行流水用于消费进度、操作意图、任务关联和恢复，不作为另一个独立库存账。写入失败按未发送、结果未知、确定成功分开；结果未知先读精确目标，有证据才续跑，不能盲重发。

必备接口语义：健康检测、身份读取、记录读取与条件核验、动作提交、动作回查、证据验证。每个接口必须说明输入、输出、错误、是否可重试、外部副作用和恢复方式。传输及字段形态待T02冻结，不编造现成端点。

## 未定工程参数

T02提出并由主控确认：审批链接实现、提醒时区/提前一天口径/发送时段、取消入口权限、配置变更对在途单影响、部署依赖锁定、运行文件存储、迁移互斥。未定不等于默认放行，也不继续扩首版业务。

## 扩展

程序在Windows运行；员工仍在钉钉操作。后续网页仅为维护控制台，读真实状态，操作仍走闸门。物资业务复用身份、读写、调度与恢复，保留自身业务规则。首版不开发网页和物资模块。
