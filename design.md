# 最小MVP设计（T02候选，待主控冻结）

业务规则以SPEC.md及Issue #2最新主控开工卡为准。contracts/已提供Python3.11+标准库类型、纯转换和注入接口，tests/contracts/仅为合成测试。借还执行、钉钉适配、持久恢复及机器排他仍是后续实现，不得将本候选写成业务程序完成。主控审查并合并后才冻结，不建设跨平台协议或微服务集群。

## 模块所有权

| 模块 | 建议目录 | 职责 | 禁止越界 |
|---|---|---|---|
| 公共定义与校验 | contracts/ | 字段、状态、错误、操作标识、闸门契约 | 不藏平台调用或个人配置 |
| 钉钉读写 | integrations/dingtalk/ | 查询、写入、待办、结果回查、身份映射 | 不决定业务状态，不代审批 |
| 部署与导入 | bootstrap/ | BAT、可见检查、导入预览、绑定 | 不绕过共用读写，不内置真实数据 |
| 借还 | workflow/ | 预留、借出、归还、关联、幂等 | 不私建另一套调度或钉钉客户端 |
| 调度与提醒 | scheduler/ | 扫描、去重、恢复、提醒 | 不直接修改库存、不代人工确认 |
| 规则预留 | rules/ | 版本和来源、未覆盖的明确状态 | 不提前做完整制度引擎 |
| 验证及发布 | tests/、docs/ | 各层测试、证据、本机部署说明 | 不声称假接口证明外部成功；迁移延期 |

T02本次仅维护contracts/、tests/contracts/、SPEC.md、design.md、tickets.md。T09独占现有检查脚本、其测试、钩子、CI和PR模板；T02不改依赖清单、bootstrap或其他业务模块。各协作者通过主控协调共用变更，不能各自扩私有字段。

## 状态与证据

主链：可信申请→awaiting_approval→reservation_pending→awaiting_issue_confirmation→borrowed→awaiting_return_confirmation→closed；另有rejected/cancelled终态。明确同意与预留是两个动作；预留冲突保持reservation_pending并报告RESERVATION_CONFLICT，不借出。

业务状态与操作结果分开：not_sent / unknown / verified / not_applied。WriteIntent.after是期望，不是现状；verify仅在借用单和库存精确回读一致时返回新状态。归还回写尚未核验时保持awaiting_return_confirmation + unknown，不能提前恢复可借量或关闭。取消只接受尚未借出的指定管理人明确指令，释放预留也走完整回读。

处理同一申请时校验原记录、阶段、配置版本、操作人；每个外部副作用绑定稳定operation_id。申请、归还记录、审批结果和阶段待办分别保存关联，不能只有一个通用完成标记。

字段、错误、接口、来源及调用顺序唯一详表见[contracts/README.md](contracts/README.md)。模型构造器不是安全沙箱；可信事件仅由ReadPort核对原单、阶段资源及源端身份后产出，不接受员工自行传verified。通讯录身份和待办内部身份分别命名；后者只按对应task的创建参数、初始回读及新增完成活动映射，不推断通用转换。

## 写入和恢复

首版只支持一台机器、一账号、一主账、一活动实例。SingleInstance是待T04实现的机器级排他接口，不是Python线程锁；不同工作目录/用户也不能重复获取同账写入权。持有期间串行处理，失去排他权立刻停止写入；重启先回查持久操作流水。跨机器并发、在途迁移与分布式互斥延期，不宣称已解决。

业务主账保持钉钉唯一权威；本地运行流水用于消费进度、操作意图、任务关联和恢复，不作为另一个独立库存账。写入失败按未发送、结果未知、确定成功分开；结果未知先读精确目标，有证据才续跑，不能盲重发。

OperationStore须在网络调用前持久保存原意图和发送开始标记，重启load原意图，不按新配置重算；同ID不同负载拒绝。未决操作拦住同单/同物资的后续写入。部分写入不算未写，不重放整笔；先query所有目标，仍不一致则挂起主控处置。多维表无事务承诺，提交前回读比较revision和业务值；管理人直接维护库存须先停自动处理并在恢复前重验。

最小接口为ReadPort、StagePort、WritePort、OperationStore、SingleInstance及RuntimeBinding校验。StagePort只建立本单受限入口/独立确认待办，不包含通用审批流；创建受理不明同样先回查。生产底层写接口每次检查排他权、当前权限和绑定，不依赖BAT或ready布尔值。接口实现、API字段映射及真实能力仍由后续任务验证，不编造端点。

## 主控待决定与已确定边界

已确定：Asia/Shanghai业务日期、提前一天一次、逾期每天一次、待归还确认暂停；取消为指定管理人、仅借出前；在途单保留创建配置，身份路由变化需显式重新确认。主控仅需一次确认contracts/README.md的09:00及不跨日补发建议，确认前不启用提醒。权限全拒绝是首版边界，不继续追完整字段矩阵。

T03验证受限原单/阶段关联可实现性，无法证明时停住，不靠可编辑标题拼接。T04/T05按接口实现本机排他和恢复记录，不在T02实现数据库或锁服务。已有T01的部分权限证据等级、故障恢复及通用关联缺口完整保留；T07真实整笔借还和关键反向路径通过前不能生产放行。

## 扩展

程序在Windows运行；员工仍在钉钉操作。后续网页仅为维护控制台，读真实状态，操作仍走闸门。物资业务复用身份、读写、调度与恢复，保留自身业务规则。首版不开发网页和物资模块。

字段级权限矩阵、通用审批框架、多机并发/迁移及埋点明确延期。规则入口版本与来源可引用，RULE_COVERAGE固定not_covered，不代表任何制度条款已合规。
