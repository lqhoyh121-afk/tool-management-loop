# 任务与主控调度

任务初始均为未开始、未指派。主控分配后才开工，禁止自动领取。GitHub Issues为执行状态入口，本文件保留范围和依赖；每张卡完成必须附测试证据。L1为隔离单元测试，L2为进程/模块集成，L3为界面操作，L4为真实钉钉业务回读。

## T01 主控本机：验证钉钉最小真实链路

**执行位置：主控本机，不外派。依赖：无。**
目标：验证收集表提交→可信身份读取→明确审批结果→指定管理人完成待办→读取真实操作者及记录关联的最短链。验证表字段权限、普通使用者能否越权改主账。数据与目标由主控授权，不能取缓存猜测。
允许改：docs/evidence（仅脱敏结论）。禁止改：业务源码、他人机器、生产记录。工具探针如需开发另由主控分配，实际运行留主控侧。
验收L4：真实记录ID、待办ID、操作者证据、精确回读；验证错人、仅勾审批待办不放行。没有实际操作者证据则报告阻塞，不伪造关联。重复运行先回查，不重复创建。测试记录清理并回读。
人工：授权、表及人员选择、真实审批和确认必须本人完成。
- [ ] 可信身份、审批结果、确认身份均有证据
- [ ] 权限隔离和清理已核验
- [ ] 不成立的能力明确提出替代建议，未悄悄降低门槛

## T02 公共约定：字段、状态与测试契约冻结

**执行位置：隔离开发，可外派。依赖：T01。**
目标：将SPEC及验证事实转换为contracts、状态转换和契约测试；提供明确标记的测试实现，使后续作者不用猜字段。主控冻结后才能作为共同依赖。
允许改：contracts/、tests/contracts/、design.md、依赖清单、CI的共用约定。禁止改：本机配置、真实台账、各业务模块。
验收L1/L2：正向借还和缺身份、错单、重复、数量不足等失败测试；规则入口明确未覆盖，版本及来源可引用。设计持久操作ID、结果未知状态、单写入者和多实例防冲突；跨机器迁移不能仅依靠本地锁。主控决定提醒时间口径、取消权限、在途配置变更。不得把设计草案写成实现完成。
人工：主控确认公共约定。提供并实际运行具体测试命令，不提交零项测试报告。此任务不写真实钉钉。
- [ ] 状态、字段、错误、幂等、恢复和接口全部有一致定义
- [ ] 其他模块可仅依赖契约开发
- [ ] 共用文件维护权明确，测试非零且通过

## T03 钉钉读写：一笔操作可提交、可查询、可回读

**执行位置：隔离开发，可外派。依赖：T02。**
目标：实现integrations/dingtalk的受控读写和身份映射；收集表记录、审批结果、确认待办和提醒均能通过共用调用访问。
允许改：integrations/dingtalk/、tests/integrations/。禁止改：业务规则、主控登录、真实群和台账。不得要求协作者使用主控凭据。
验收L1/L2：注入假传输验证参数和解析、限流、权限失败、超时未知结果、阶段关联；每项写入返回待核验或有回读证据的结果，不能只看退出码。真实验证由T07主控执行，隔离测试通过不能声称L4。清理仅限测试产物。
人工：无真实授权操作；交接列出主控后续实测命令、所需权限和停止点。
- [ ] 解析真实T01字段形态，内部人员ID不误当通讯录ID
- [ ] 写入不明先查询，重复调用不盲重发
- [ ] 没有个人路径、账号或表ID

## T04 部署向导：从文件选择到就绪检查

**执行位置：隔离开发，可外派；本机部署由T07执行。依赖：T02。**
目标：实现BAT入口、终端状态面板、Windows文件选择框、官方授权引导、台账导入预览和确认、人员及路由配置；对接共用读写契约。真实联调依赖T03。
允许改：bootstrap/、tests/bootstrap/、docs/deployment.md。禁止改：主控环境、全局配置、真实台账、钉钉读写核心。
验收L1/L2/L3：HTML伪装xls和真实Excel识别；缺字段不编造；环境、身份、台账和权限分别显示。未确认不写入，绕过BAT直接调用也被底层闸门拒绝。重复部署不覆盖既有配置或业务，密钥不进日志。支持中文和空格路径，失败界面不闪退。
人工：目标使用者选择文件、授权及确认绑定；测试用隔离输入，不让Agent代选真实人员。回读用注入测试实现验证，标明非真实部署。
- [ ] 首次配置和中断恢复流程可见
- [ ] 缺必要条件不能启动；ready标记不能绕过
- [ ] 交付主控实机部署及回退说明

## T05 借还闭环：申请至归还的受控状态推进

**执行位置：隔离开发，可外派。依赖：T02。**
目标：通过公共接口实现单品种多件、同意后预留、管理人借出确认、整笔归还和管理人确认关闭。真实联调依赖T03。
允许改：workflow/、tests/workflow/。禁止改：部署、钉钉连接层、提醒策略、他人定义的字段。
验收L1/L2：错人、错单、旧阶段、缺编号和不一致数量均不放行；重复动作不重复增减；预留竞争不超发；操作流水支持重启和部分成功恢复。拒绝不借出，取消按T02权限释放；不扩延期和退回等后置规则。
人工：审批及完成信号仅消费可信外部事件，不代人生成。外部写入使用契约测试实现，不声称本机真实验收完成。
- [ ] 正常闭环及拒绝路径可复现
- [ ] 并发预留和未知写入结果有测试
- [ ] 回读未确认不能关闭借用

## T06 扫描提醒：按状态触发且重启不重复

**执行位置：隔离开发，可外派。依赖：T02、T05。**
目标：扫描新记录驱动受控流程；提前1天提醒、逾期每天一次、归还待确认暂停、不催管理员。实际联调依赖T03。
允许改：scheduler/、tests/scheduler/。禁止改：库存、人员权限、审批和确认状态实现。
验收L1/L2：注入时钟验证T02冻结的日期边界、重复扫描、断网、重启及发送结果不明回查；不同记录不串单。调度进程可见启停，状态持久化，不能关闭聊天就丢失任务。测试不改生产系统时间、不发真实消息。
人工：无。交接真实提醒测试方法，由主控授权隔离目标后执行。
- [ ] 指定提醒规则精确满足
- [ ] 不存在管理员催办或重复补发风暴
- [ ] 停止、重启和待确认暂停均有证据

## T07 主控本机：首次部署与真实端到端验收

**执行位置：主控本机，不外派。依赖：T01、T03、T04、T05、T06。**
目标：从合并后的仓库内容通过BAT完整部署；不因已有依赖或登录而跳过检查。按SPEC由真人走完借还和提醒，验证关联和主账结果。
允许改：docs/evidence的脱敏摘要、测试说明。实际配置及数据只存在本地忽略目录和授权钉钉目标；不在验收卡里静默重写模块，问题退回对应任务。
验收L3/L4：真实身份、授权确认、原单ID和各阶段待办ID、提醒送达及每日防重、归还等待暂停、管理员不催办、最终数量和状态回读。反向验证错人、错单、重复启动、缺配置及写入中断。
人工：主控部署绑定；真实申请、审批、借出和归还确认由各人员完成。脚本不得代勾。清理隔离记录并回读，保留最小脱敏证据，不把凭据或内部单据公开。
- [ ] 本机真实闭环通过
- [ ] 反向闸门和测试清理通过
- [ ] 问题归属清楚；本机通过不冒充异机通过

## T08 主控侧：异机迁移与发布验收

**执行位置：主控侧另行安排，不自动外派。依赖：T07。**
目标：验证另一台Windows服务器按说明接入；分清新实例初始化和迁移在途业务，代码不含个人配置。
允许改：docs/release.md及脱敏迁移报告。禁止改：未经授权的目标机器、生产任务或Git历史。
验收L3/L4：缺环境可见阻断、路径可迁移、真实身份重新绑定；迁移原台账时先停旧实例，核对在途任务和待办，再启新实例；不能两个服务器重复消费。同一业务外部成功动作不能重做。失败时停新实例并按核验结果恢复，不把代码回滚等同外部操作撤销。
人工：目标环境及身份授权、迁移启停和发布由主控确认；无目标机权限则保持未完成，不伪造验证。
- [ ] 干净受支持环境验证通过
- [ ] 数据、待办和消费主权切换可核对
- [ ] 发布物无真实数据及个人配置
