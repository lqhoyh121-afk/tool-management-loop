# 贡献与主控调度

仓库所有者担任主控。两个协作者的邀请不等于任务指派，先在Issue中由主控确认负责人和范围，再开分支。

1. 读取SPEC、design、任务卡和AGENTS。不得改变首版范围。
2. 安装本地钩子，针对任务开独立分支，不直接向main推送业务改动。
3. 只改任务授权目录；共用定义有缺口，先向T02维护者提变更，不私建旁路。
4. 提交PR，关联Issue，提供实际测试命令、非零测试结果、失败边界和外部副作用说明。
5. 主控审核后决定是否合并。不要自动合并或自行关闭需要主控真实验收的卡。

主控本机部署、钉钉授权、真实台账导入、实际接口探测及最终联调不派给协作者。协作者可写工具，在隔离环境验证，不索要主控凭据。

所有公开材料不得含个人署名、个人绝对路径、真实人员/群/表ID、内部台账、访问链接或密钥。源码中的合成测试标识必须显式标明。Git作者元数据正常保留。

仓库guard是保守检查，不覆盖所有敏感信息形态；提交者必须人工检查diff，后续可加入专用秘密扫描。CI通过只代表仓库检查通过，不代表业务可用。本项目暂无完整业务程序。

主控审核是协作规定；仅配置PR和CI门禁并不能保证只有主控能点合并，平台权限及更细合并限制须另行验证。不得声称文档约定自动成为平台权限。
