# 协作约束

## 适用范围
所有协作者、自动化代理和仓库交付物均遵循本文件。需求尚处讨论阶段；未确认的业务范围不得自行实施。

## 可移植性与隐私
- 源码、文档、模板、消息及生成产物不得添加个人署名。
- 不得硬编码开发者个人绝对路径、账号、凭据或机器配置。
- 文件路径从项目根目录、配置项或运行时输入解析，不依赖开发机当前目录。
- 配置示例仅包含非敏感示例；真实凭据、运行数据、日志、缓存、备份不进入Git。
- 不修改或伪造Git作者历史。署名规则针对交付内容，不要求抹除Git协作审计记录。

## 执行原则
- 确定性流程优先脚本化：校验、状态流转、调度、提醒、防重、外部读写及核验。
- Agent仅用于经需求确认确实必要的语义或非结构化判断节点；必须定义输入、输出、超时和失败处置。
- Agent不得绕过权限、固定业务规则及人工审批。
- 外部写入受理不明时先回查，不盲目重发；只有回读证据完整才报告成功。

## 提交与验证
- 按任务隔离改动；不得夹带真实业务数据或本地备份。
- 项目建立后配置提交前检查及GitHub CI：敏感信息、个人路径、署名、格式、相关测试和适用的静态检查。
- 本地钩子可被跳过，远程CI和受保护分支才是多人合并约束；未配置完成不得声称已强制启用。
- 技术栈和目标运行环境确认后选择钩子工具，不为使用Husky强行增加业务运行时依赖。
- 未获明确授权，不自动提交、推送或修改远程仓库设置。
